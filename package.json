{
  "name": "mcp-x",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "mkdir -p dist && cp mcp-x.config.json dist/ && tsc",
    "dev": "mkdir -p dist && cp mcp-x.config.json dist/ && tsx watch src/index.ts",
    "start": "mkdir -p dist && cp mcp-x.config.json dist/ && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "zod": "^3.25.76",
    "express": "^4"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
